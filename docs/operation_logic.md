# 人格升华操作逻辑

本文件保留为兼容入口；当前统一设计以 [`unified_design.md`](unified_design.md) 为准。

核心路径只有一条：

```text
选人格 → 配模块 → 生成调整草案 → 看差异 → 审批/应用到人格
```

只有“调整草案”能进入最终写回。观察、模块、版本记录、请求捕获和可选备注都只是辅助数据，不能直接修改 AstrBot 原版 persona。LLM Tools 只允许查询、记录和起草，不暴露审批、应用、删除、cleanup 或 debug 能力。
